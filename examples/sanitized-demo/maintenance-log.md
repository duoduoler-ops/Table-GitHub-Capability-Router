# 演示维护日志

## 2026-01-01

- 根据脱敏输入 `demo-environment-v1` 创建 5 行能力账本、4 类 L1 路由和 1 张最小能力卡。稳定 ID 查重通过；未发现重复行。`site-publisher` 只登记为 disabled，没有启用或调用。未安装、登录、外发、删除或修改配置。由于没有实际命令证据，`local-shell` 和 `public-doc-search` 保持 `unverified`。
