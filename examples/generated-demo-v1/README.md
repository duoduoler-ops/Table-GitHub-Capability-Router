# Generated Demo v2 / 自动生成演示 v2

This is a fictional, publish-safe workflow root generated by `scripts/workflow.py`, then transactionally migrated to schema v2 on 2026-07-31. It is executable evidence of the output shape, not a claim that the example repositories or capabilities exist.

这是由 `scripts/workflow.py` 实际生成、并于 2026-07-31 事务化迁移到 Schema v2 的虚构可公开工作流根目录。它证明输出结构、迁移和状态门禁能工作，不代表示例项目或能力真实存在。

The directory name remains `generated-demo-v1` to preserve public links; the current records and `workflow.json` are schema v2.
目录名继续保留 `generated-demo-v1` 以避免公开链接失效；当前记录和 `workflow.json` 已是 Schema v2。

## Five golden records / 5 个黄金样例

1. B-grade reference project: `gh-example-labs-reference-patterns`, with one thin-discovery row and one full-semantic row.
2. D-grade rejected project: `gh-example-labs-all-tools-manager`, excluded from both generated sections.
3. Healthy, approved, conditionally routed capability: `public-doc-checker`.
4. Quarantined capability, absent from the router: `unsafe-importer`.
5. Disabled manager-type capability, absent from the router: `session-manager`.

## Verify / 验证

From the repository root, run:

```powershell
python scripts/workflow.py validate --root examples/generated-demo-v1
```

Expected result: `projects: 2`, `semantic_projects: 1`, `capabilities: 3`, `errors: 0`.

The `.workflow/transactions/` directory contains real relative-path transaction manifests and pre-change backups produced by the demo run. No installation, login, publishing, deletion, or client configuration change was performed.

`.workflow/transactions/` 保存本次演示真实生成的相对路径事务清单和修改前备份。本演示没有安装、登录、外发、删除或修改客户端配置。
