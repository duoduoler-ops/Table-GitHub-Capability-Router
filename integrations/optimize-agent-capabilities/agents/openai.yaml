interface:
  display_name: "Audit Agent Capabilities"
  short_description: "Read-only agent capability inventory"
  default_prompt: "Run a read-only audit of this agent's capability routing. Do not modify files or configuration."

policy:
  allow_implicit_invocation: false
